<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <link rel="icon" type="image/png" href="/images/logoImg/icon.png" />
    <title><%= title %></title>
    <link rel="stylesheet" href="stylesheets/style.css" />
    <!-- Toastify CSS -->
    <link rel="stylesheet" href="/toastify/toastify.css" />
    <!-- fonts setup -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400..700;1,400..700&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap"
      rel="stylesheet"
    />
    <!-- loader -->
    <link rel="stylesheet" href="/node_modules/loaders.css/loaders.min.css" />
    <!-- Meta data -->
    <!-- Primary Meta Tags -->
    <meta name="title" content="<%= title %> | YourSiteName" />
    <meta name="description" content="<%= description %>" />
    <meta
      name="keywords"
      content="YourSiteName, login, signup, account, authentication, secure login"
    />
    <meta name="robots" content="noindex, nofollow" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content="<%= pageUrl %>" />
    <meta property="og:title" content="<%= title %> |  Engineer Insights" />
    <meta property="og:description" content="<%= description %>" />
    <meta property="og:image" content="/images/logoImg/icon.png" />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary" />
    <meta property="twitter:url" content="<%= pageUrl %>" />
    <meta property="twitter:title" content="<%= title %> |  Engineer Insights" />
    <meta property="twitter:description" content="<%= description %>" />
    <meta property="twitter:image" content="/images/logoImg/icon.png" />
  </head>
  <script
    async
    src="https://www.googletagmanager.com/gtag/js?id=G-FG5BN3EY26"
  ></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag() {
      dataLayer.push(arguments);
    }
    gtag("js", new Date());
    gtag("config", "<%= user.googleAnalyticsId %>", {
      user_id: "<%= user._id %>",
    });
  </script>
  <body
    class="min-h-screen bg-cover bg-center mt-0 pt-0 <%= user.settings.theme === 'dark' ? 'text-dark-text' : 'text-text' %>"
    style="<%= `background-image: url(/images/backgroundImages/${user.settings.theme === 'dark' ? 'darkbackground.jpg' : 'background.png'})` %>"
  >
    <%- include("../partials/profileHeader.ejs") %><%- include(view) %> <%-
    include("../partials/loader.ejs") %> <%- include("../partials/footer.ejs")
    %>
  </body>
</html>
<script src="/toastify/toastify.js"></script>
